import 'dart:ui';

class AppColors {
  // Primary palette (Instagram-like gradient colors)
  static const primary = Color(0xFFE1306C); // Insta Pink
  static const primaryLight = Color(0xFFF56040); // Insta Orange
  static const primaryDark = Color(0xFF833AB4); // Insta Purple
  static const accent = Color(0xFFFCAF45); // Insta Yellow

  // Semantic
  static const income = Color(0xFF00C853);
  static const expense = Color(0xFFFF3D00);
  static const warning = Color(0xFFFFD600);
  static const info = Color(0xFF00B0FF);

  // Dark theme (True Black for OLED/Instagram look)
  static const darkBg = Color(0xFF000000);
  static const darkSurface = Color(0xFF121212);
  static const darkCard = Color(0xFF1E1E1E);
  static const darkText = Color(0xFFFFFFFF);
  static const darkSubtext = Color(0xFFAAAAAA);

  // Light theme
  static const lightBg = Color(0xFFFFFFFF);
  static const lightSurface = Color(0xFFFAFAFA);
  static const lightCard = Color(0xFFFFFFFF);
  static const lightText = Color(0xFF262626);
  static const lightSubtext = Color(0xFF8E8E8E);

  // Glassmorphism
  static const glassWhite = Color(0x0AFFFFFF);
  static const glassBorder = Color(0x1AFFFFFF);
  static const glassDark = Color(0x0A000000);

  // Category colors
  static const categoryFood = Color(0xFFFF5252);
  static const categoryShopping = Color(0xFFE040FB);
  static const categoryTransport = Color(0xFF448AFF);
  static const categoryEntertainment = Color(0xFFFF4081);
  static const categoryBills = Color(0xFFFFAB40);
  static const categoryHealth = Color(0xFF69F0AE);
  static const categorySalary = Color(0xFF00E676);
  static const categoryOther = Color(0xFF9E9E9E);
}
